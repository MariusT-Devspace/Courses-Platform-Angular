import { DecodedTokenPipe } from './decoded-token.pipe';

describe('DecodedTokenPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodedTokenPipe();
    expect(pipe).toBeTruthy();
  });
});
